#!/bin/bash

export_extensions_settings() {
    dconf dump /org/gnome/shell/extensions/$1/ #>~/.config/dconf/extensions/$1
}

extensions=($(/usr/bin/ls -1 ~/.config/dconf/extensions/))

for extension in ${extensions[@]}; do
    export_extensions_settings $extension
done
