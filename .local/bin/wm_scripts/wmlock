#!/bin/bash


while getopts "o" option; do 
	case "${option}" in
		o) i3lock -e -f -c 000000 -i ~/.config/wall.png -t && sleep 5 && xset dpms force off ;;
	esac 
done

i3lock -e -f -c 000000 -i ~/.config/wall.png -t 
